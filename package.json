{
  "name": "kigen-shinobi",
  "private": true,
  "version": "1.0.0",
  "description": "Wiki + sistema dinâmico Kigen Shinobi (HTML estático + Supabase)",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "node scripts/typecheck.mjs",
    "generate:config": "node scripts/generate-config.mjs",
    "build": "npm run generate:config && node scripts/validate-build.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "vite": "^6.3.5"
  }
}
